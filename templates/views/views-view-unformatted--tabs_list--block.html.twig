{% if title %}
  <h3>{{ title }}</h3>
{% endif %}
  
<div class="slider responsive carousel-vertical-scroll">
    {% for row in rows %}
      <div class="views-row" {{ row.attributes }}>
        {{ row.content }}
      </div>
    {% endfor %}    
</div>  
